<h1 style=" color: rebeccapurple;">elevate</h1>
<%= @pizza %>
when you're DOWN, just look up. when you're UP, just keep going...
<% @tips.each do |tip| %>
  <%= tip.lesson %>
  <!-- ^outputs lesson^ -->
<% end %>

<div class="booyah-box col-10 offset-1">
  <h1>Enter Your reflection</h1>
  <%= simple_form_for @tip do |f| %>
    <%= f.input :lesson %>
    <!-- form -->
    <%= f.submit 'Create', class: 'btn btn-primary' %>
    <!-- ^button^ -->
  <% end %>

</div>
<div class="background-image">
  <!-- ^ image css'd to master scss -->
  </div>
